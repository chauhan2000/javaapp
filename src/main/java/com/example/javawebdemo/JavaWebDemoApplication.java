package com.example.javawebdemo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;


@SpringBootApplication // This is the main class that starts the application
public class JavaWebDemoApplication {
    // This is the main method that starts the application
    public static void main(String[] args) {
        SpringApplication.run(JavaWebDemoApplication.class, args);
    }
}
